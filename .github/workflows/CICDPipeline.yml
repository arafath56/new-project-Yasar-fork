name: Learning CI/CD Pipeline

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  # ---------- CI Job ----------
  ci:
    name: Continuous Integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up environment
        run: echo "⚙️ Setting up environment..."

      - name: Run tests
        run: echo "✅ Running tests (nothing to run yet, just learning)"

  # ---------- CD Job ----------
  cd:
    name: Continuous Deployment
    runs-on: ubuntu-latest
    needs: ci
    # 👇 CD only runs on push events (not PRs)
    if: github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Fake deployment
        run: echo "🚀 Pretend deployment successful!"
